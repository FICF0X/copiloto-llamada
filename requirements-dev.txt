# Packaging CallAssist into a standalone Windows .exe (see build_exe.bat).
# Not needed to run from source - only to build the distributable.
pyinstaller==6.22.0
pyinstaller-hooks-contrib==2026.6

# Test suite (tests/). Not bundled - CallAssist.spec never references it.
pytest==8.3.4
